# Claude Opus 5.5 code review

Three grounded, read-only Claude CLI reviews of the local worktree completed on 23 September 2026 with model `claude-opus-5-5`. All returned successful result events. Claude inspected targeted diffs and files; it did not edit files or run tests. One broad Bash inspection request in each of the first two passes was denied by the read-only tool restrictions, so claims below are limited to the files it could inspect.

The first pass found no blocking calculation bug. It did find that the standalone `vault-analysis-json.py` wrapper did not configure INFO logging, which would hide the new phase records. It also flagged benchmark provenance, the two-bucket condition for Brotli savings, and the output-order caveat as unclear. The wrapper now calls `run_cli()`, which configures logging; the [README](../../../scripts/erc-4626/README-vault-scripts.md) documents the behaviour and both scratch state paths. The [results](implementation-results.md) now distinguish paired local runs from the earlier baseline and estimated compression savings from measured compression cost. A recursive [ordering comparison](top-projection-ordering.json) records the exact remaining JSON differences.

The second pass reported **no blocking correctness issue or materially misleading documentation** in the reviewed files. It confirmed the wrapper logging path, scratch state variable, changed-column write-back rule and benchmark caveats. A third, focused pass checked the `main()` logging conversion and also reported no blocking issue. It found one dead comment and two remaining helper `print()` paths; those were removed or routed through the logger. Claude did not trace every downstream use of the excluded Parquet columns or run a production cycle; the full local export comparison and deployment validation remain the evidence for those areas.

A fourth grounded review of PR #1595 against `origin/master` completed with `claude-opus-5-5` and a successful result event. It found no correctness bug. It identified an operational visibility change: the scanner's default WARNING console level hides the new INFO phase lines from `docker compose logs`, while its rotating `logs/scan-all-chains.log` file records them. The README and performance notes now name that file. It also found a duplicate output-directory creation, which was removed. Its other suggestions were not applied: the optional Brotli payload supplier preserves direct one-bucket calls, Unix `resource` is acceptable for this Linux scanner, and the saved benchmark artefacts were explicitly requested. Projection parity remains supported by the recorded full local A/B rather than a small synthetic unit test; production validation is still pending.

The local follow-up also corrected the exporter's path defaults and stale module examples. An explicit `data_dir` now anchors the metadata, Parquet and public-output defaults, while a standalone call still reads `DATA_DIR` and `OUTPUT_JSON`. All 37 focused sticky-export tests passed with the test token-cache seed disabled because the shared temporary cache file was not writable. Claude reviewed the preceding committed PR state; these follow-up edits were verified locally after its result.

A fifth, focused Opus 5.5 review inspected those local edits. It confirmed the path behaviour and AGENTS.md compliance, then found that two new documentation sentences wrongly implied byte parity against the older exporter. The correction now names the two new follow-up outputs as the byte comparison set and records the seven changed equal-time curator post selections separately. Its suggestion to simplify and rename the default-path helper was also applied. This final adjustment was checked with focused tests and formatting after the review.

A sixth, grounded Opus 5.5 review inspected the private stablecoin daily-preparation change and returned a successful result event with no blocking correctness bug. It highlighted weak reproducibility of the initial 100-vault comparison, missing final JSON parity, a possible duplicate-day failure mode and an unnecessary retained source frame. The follow-up [benchmark](benchmark-private-daily.py) now fixes the clock and verifies byte equality for the complete metadata and both state files across 500 stablecoin vaults. The duplicate-day exception reports the affected date and count, the wide stablecoin frame is released before the metric loop, and a Parquet-backed Arrow NaN regression test was added. Production-cycle performance remains unmeasured.
