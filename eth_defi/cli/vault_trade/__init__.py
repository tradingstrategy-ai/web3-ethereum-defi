"""Reusable helpers for vault deposit and redemption test runs.

The package deliberately keeps its modules short and unprefixed: callers use
``eth_defi.cli.vault_trade.core``, ``.state``, ``.simulation``, ``.setup``,
``.runner`` and ``.tui``.
"""
