"""DEX aggregator router identification and calldata decoding.

Used for execution-quality research: given a swap transaction's call trace,
find which aggregator routed the trade and what slippage bound the user gave.
"""
